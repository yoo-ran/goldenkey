import type { TypedEvent } from '../../util/observable';
import { BaseManager } from '../baseManager';
import type { ChartContext } from '../chartContext';
import { type Series } from './series';
/** Manager that handles all top-down series-area node-click related concerns and state. */
export declare class SeriesAreaClickManager extends BaseManager {
    private readonly id;
    private readonly chart;
    private readonly ctx;
    private series;
    private lastHover?;
    private seriesRect?;
    constructor(id: string, chart: {
        fireEvent<TEvent extends TypedEvent>(event: TEvent): void;
    }, ctx: ChartContext);
    seriesChanged(series: Series<any, any>[]): void;
    dataChanged(): void;
    preSceneRender(): void;
    private update;
    private layoutComplete;
    private onLeave;
    private onHover;
    private onClick;
    private checkSeriesNodeClick;
}
